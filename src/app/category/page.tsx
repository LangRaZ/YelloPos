import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Category Overview',
  }

export default function CategoryOverviewPage() {
  return (
    <div>category overview page</div>
  )
}
